[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
# A test that genuinely needs the internet says so; everything else is refused
# a socket by tests/conftest.py. Registered here so the marker is not a typo
# away from silently doing nothing.
markers =
    network: test makes a real outbound network call
